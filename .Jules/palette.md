## 2026-09-04 - [Disabled Button Tooltip]
**Learning:** React component states can disable buttons dynamically, and providing a fallback tooltip using the native `title` attribute greatly improves accessibility and user understanding of why an interaction is unavailable.
**Action:** Always verify if a disabled state can benefit from a contextual tooltip.
