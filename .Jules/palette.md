## $(date +%Y-%m-%d) - Enhance button keyboard accessibility

**Learning:** Some custom native interactive elements (like `<button>`) lack visual focus rings for keyboard navigation.
**Action:** Added Tailwind `focus-visible` ring classes to `<button>` elements in apps/web/app/room/create/page.tsx to ensure consistent keyboard accessibility.
